services:
  - type: web
    name: ecochain-backend
    runtime: node
    plan: free
    region: virginia
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
